# Crabgrass Backend Configuration

# Database
DATABASE_PATH=./data/crabgrass.duckdb

# File storage for JJ repositories
STORAGE_ROOT=./data/ideas

# Gemini API key (required for AI features in later slices)
GEMINI_API_KEY=your-api-key-here

# Server
HOST=0.0.0.0
PORT=8000
DEBUG=true

# CORS origins (comma-separated in env, but we use list in config)
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
